# Copy to .env.local for local dev (defaults to http://127.0.0.1:8000/api if unset).
# On Vercel, set this in Project Settings -> Environment Variables to your
# deployed backend's URL, e.g. https://spectacle3d.onrender.com/api
VITE_API_BASE_URL=http://127.0.0.1:8000/api
